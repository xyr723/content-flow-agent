{
  "name": "content-flow-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:test": "tsc -p tsconfig.test.json",
    "test": "npm run build:test && node --test tests/core/*.test.js tests/skills/*.test.js tests/workflows/*.test.js",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
